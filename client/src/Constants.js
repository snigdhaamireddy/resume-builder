import { createTheme } from "@mui/material";

export const lightTheme = createTheme({
    palette: {
      primary: {
        main: "#F55533",
      },
      secondary: {
        main: "#4b1f16",
      },
      background: {
        main: "#fafafa",
      },
    },
    typography: {
      fontFamily: "Poppins",
    },
});